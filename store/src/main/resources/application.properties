# Spring application name
spring.application.name=store

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/suppstore?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=horhor2004
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Connection Pooling Configuration
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.connection-timeout=30000

# Server configuration
server.port=8080

# Email Configurations (use Mailtrap or other SMTP provider if needed)
spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=e89d5b565e22d9
spring.mail.password=1567ea96ecafc6
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Dynamic From Email Address (can be different for production)
spring.mail.from=orders@supplements-store.com